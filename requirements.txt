# Core web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0
jinja2>=3.1.2
python-multipart>=0.0.6

# HTTP / scraping
httpx>=0.25.0
newspaper4k>=0.9.3
lxml[html_clean]>=4.9.0
news-please>=1.5.35
beautifulsoup4>=4.12.0
brotli>=1.0.9

# Database
psycopg2-binary>=2.9.0

# Utilities
cryptography>=41.0.0
psutil>=5.9.0

# NOTE: The following packages are intentionally NOT included in the default image
# because they add 5-8 GB (torch/ML libs + Chromium browser):
#
#   scrapegraphai>=1.0.0   → requires OpenAI/LLM key; pulls torch, langchain, transformers
#   playwright>=1.40.0     → requires 'playwright install chromium' post-install
#
# To use them locally, install via:   pip install -r requirements-optional.txt
# The API code handles both packages as optional (ImportError is caught gracefully).
